<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create(config('adobe-pdf.tracking.table', 'adobe_pdf_processes'), function (Blueprint $table) {
            $table->id();
            $table->uuid('uuid')->unique();

            $table->string('operation')->index();
            $table->string('status')->index();

            $table->json('options')->nullable();
            $table->json('inputs')->nullable();
            $table->json('asset_ids')->nullable();

            $table->text('adobe_location')->nullable();
            $table->unsignedInteger('poll_attempts')->default(0);

            $table->string('output_disk')->nullable();
            $table->text('output_path')->nullable();

            $table->json('error')->nullable();

            $table->timestamp('started_at')->nullable();
            $table->timestamp('completed_at')->nullable();
            $table->timestamp('failed_at')->nullable();

            $table->timestamps();
        });
    }

    public function down(): void
    {
        Schema::dropIfExists(config('adobe-pdf.tracking.table', 'adobe_pdf_processes'));
    }
};
